  <header class="navbar navbar-inverse navbar-fixed-top wet-asphalt" role="banner">
      <div class="container">
          <div class="navbar-header">
              <ul class="nav navbar-nav navbar-left">
                  <li class="active"><%= link_to @diputado.nombre, diputado_path(@diputado) %></li>
              </ul>
          </div>
          <div class="collapse navbar-collapse">
              <ul class="nav navbar-nav navbar-right">
                  <li><%= link_to "Ayuda", help_path %></li>
                  <li><%= link_to "Quienes Somos?", about_path %></li>
                  <li><%= link_to "Cerrar Sesion", signout_path, method: "delete" %></li>
              </ul>
          </div>
      </div>
    </header><!--/header-->


<p>.</p>
<p>.</p>
<p>.</p>


<!--/header
            <p>Nombre de Usuario: <%=@diputado.nombre%></p>
            <p>Email: <%=@diputado.email%></p>
            <p>Descripcion: <%=@diputado.descripcion%></p> -->

<section id="help" class="container">
        <div class="panel panel-success">

          <div class="panel-heading">
            <h3 class="panel-title">Las preguntas que me han hecho (<%= @preguntas.count %>)</h3>
          </div>
				
          <div class="panel-body">
				
            <div align="center" style="height: 405px; overflow: auto;">
					
              <table class="table table-striped">
                <tr>		
                  <th>Nombre</th>
                  <th>Respuesta</th>
                  <th>Responder</th>
                </tr>

                <% if @diputado.preguntas_diputados.any? %>
                <% @preguntas.each do |pregunta| %>

                    <tr>
                      <td><%= pregunta.pregunta %></td>
                      <td><%= pregunta.respuesta %></td>
                         
                      <td>
                        <span class="glyphicon glyphicon-pencil"></span>
                      </td>
                   </tr>

                <%end %>

              </table>
            </div>
          </div>
        </div>
      <% end %>

</section>


